package com.spirit.shit.block.custom;

/*public class BackroomsBrokenLightBlock extends Block {
    public BackroomsBrokenLightBlock(Settings settings) {
        super(settings);
    }

    public void randomDisplayTick(BlockState state, World world, BlockPos pos, Random random) {
            if (random.nextInt(50) == 0) {
                world.playSound((double)pos.getX() - 0.5, (double)pos.getY() - 0.5, (double)pos.getZ() - 0.5, ShitSounds.LIGHT_FLICKER, SoundCategory.BLOCKS, 0.5F + random.nextFloat(),  0.8F, true);
        }
    }
}
*/